global:
  scrape_interval: 15s

scrape_configs:
  - job_name: suitepilot-backend
    metrics_path: /api/health/metrics
    static_configs:
      - targets:
          - backend:3001

  - job_name: prometheus
    static_configs:
      - targets:
          - localhost:9090
