# Shared Packages

Reserved for shared SuitePilot packages such as typed contracts, reusable validation helpers, SDK clients, and design tokens.

Rules:

- Package code must be consumed by at least two workspaces before being promoted here.
- Keep domain source of truth in Prisma schema and SSOT documents.
- Avoid creating a package for a one-off helper.
