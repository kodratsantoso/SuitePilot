# SuitePilot Documentation

This directory contains operator-facing notes for the SuitePilot application.

Current coverage:

- Docker and environment validation live in `.ssot/architecture/DEPLOYMENT.md`.
- API behavior and validation live in `.ssot/architecture/API_CONTRACTS.md` and `.ssot/validation/API_VALIDATION.md`.
- Product governance, review gates, RAG, and evaluation rules live in `.ssot/`.

Future documents should reference SSOT files instead of duplicating source-of-truth decisions.
